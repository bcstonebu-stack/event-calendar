# Frontend API URL - IMPORTANT: This is baked into the frontend build!
#
# DEPLOYMENT SCENARIOS:
#
# 1. Local Docker deployment (accessing via http://localhost):
#    VITE_API_URL=http://localhost:3001/api
#    → Works because both ports are exposed on localhost
#
# 2. Remote/Production deployment (accessing via http://your-domain.com or IP):
#    VITE_API_URL=http://your-domain.com:3001/api
#    → Or use a reverse proxy (recommended) and set to https://your-domain.com/api
#
# 3. LAN deployment (accessing via http://192.168.x.x):
#    VITE_API_URL=http://192.168.x.x:3001/api
#    → Replace with your server's actual IP address
#
# NOTE: After changing this value, you MUST rebuild the frontend:
#       docker-compose up -d --build frontend
#
VITE_API_URL=http://localhost:3001/api

# Database Configuration (used by docker-compose)
POSTGRES_USER=calendar_user
POSTGRES_PASSWORD=change_this_password_in_production
POSTGRES_DB=event_calendar

# Backend API Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
# For local development: *
# For production: https://your-domain.com
CORS_ORIGIN=*
